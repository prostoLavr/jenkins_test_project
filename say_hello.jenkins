#!groovy

properties([dissableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '5'))
        timestamps()
    }
    stages {
        stage("Say Hello"){
            steps {
                sh 'ssh lawrence@lava-land.ru "echo Hello World"'
            }
        }
    }
}
