#!groovy

properties([dissableCurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '5'))
        timestamps()
    }
    stages {
        state("Say Hello"){
            steps {
                'ssh lawrence@lava-land.ru "echo Hello World"'
            }
        }
    }
}
